import React from 'react'

const Header = ({props}) => {
    const header = props;
    return (
        <div>
            <header>{header}</header>
            
        </div>
    )
}

export default Header
